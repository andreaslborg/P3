<div class="modal @ModalClass" tabindex="-1" role="dialog" style="display:@ModalDisplay">
    <div class="modal-dialog modal-xl" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Ofte Stillede Spørgsmål</h5>
                <button @onclick="() => Close()" type="button" class="close btn btn-secondary" data-dismiss="modal" aria-label="Close">
                    <i class="bi bi-x-lg"></i>
                </button>
            </div>
            <div class="modal-body">
                <div>
                    <p class="fw-bold">Hvordan opretter jeg en ny informationstavle?</p>
                    <p>En ny eller ekstra informationstavle skal tilkøbes. Kontakt Mikkel på mwp@taldigital.dk for at høre mere.</p>
                    <br />
                    <p class="fw-bold">Hvordan redigerer jeg et opslag?</p>
                    <p>Tryk på <i class="bi bi-pencil"></i> ud fra det opslag du gerne vil redigere. Husk at afslutte med Gem.</p>
                    <br />
                    <p class="fw-bold">Hvordan ændrer jeg rækkefølgen på mine kategorier?</p>
                    <p>Tryk på Rediger kategorier og anvend pilene i højre side. Den øverste er den som står først på informationstavlen.</p>
                    <br />
                    <p class="fw-bold">Hvordan tilføjer jeg en redaktør?</p>
                    <p>Denne funktionalitet er undervejs og ikke udgivet endnu.</p>
                    <br />
                    <p class="fw-bold">Har du brug for mere hjælp?</p>
                    <p>Kontakt vores support på info@taldigital.dk</p>
                </div>

                <!-- Her kan der indsættes en video.
                <p>
                    <iframe width="1000" height="500" src="https://www.youtube.com/embed/dQw4w9WgXcQ"></iframe>
                </p>
                -->
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal" @onclick="() => Close()">Luk</button>
                <button type="button" class="btn btn-primary">Gem</button>
            </div>
        </div>
    </div>
</div>


@if (ShowBackdrop)
{
    <div class="modal-backdrop fade show"></div>
}


@code {
    public string ModalDisplay = "none;";
    public string ModalClass = "";
    public bool ShowBackdrop = false;

    public void Open()
    {
        ModalDisplay = "block;";
        ModalClass = "Show";
        ShowBackdrop = true;
        StateHasChanged();
    }

    public void Close()
    {
        ModalDisplay = "none";
        ModalClass = "";
        ShowBackdrop = false;
        StateHasChanged();
    }
}
